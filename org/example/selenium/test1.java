package org.example.selenium;

import net.bytebuddy.asm.Advice;
import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebElement;
import org.testng.annotations.Test;

import java.util.List;


//public class test1 extends baseclass{
   // @Test
  //  public void test(){
     //  driver.get("https://www.amazon.in/");
//driver.findElement(By.id("twotabsearchtextbox")).sendKeys("laptops", Keys.ENTER);
       // driver.findElement(By.linkText("Amazon miniTV")).click();
        //driver.findElement(By.name("field-keywords")).sendKeys("laptops", Keys.ENTER);
       // driver.findElement(By.partialLinkText("Amazon")).click();


      //  java.util.List<WebElement> aTags = driver.findElements(By.tagName("a"));
      //  System.out.println(aTags.size());

        //for(int i=0;i<=aTags.size();i++)
        //{
          //  System.out.println(aTags.get(i).getAttribute("href"));
        //}
        //driver.findElement(By.xpath("//span[@id='nav-link-accountList-nav-line-1']")).click();



    //    driver.get("https://www.google.com/");
      //  driver.findElement(By.name("q")).sendKeys("Selenium jobs", Keys.ENTER);

  //  }
//}
